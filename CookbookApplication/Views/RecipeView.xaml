<Window x:Class="CookbookApplication.Views.RecipeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookbookApplication.Views"
        mc:Ignorable="d"
        Title="RecipeView" MinHeight="500" MinWidth="900" WindowStartupLocation="CenterScreen" Height="500" Width="900">
    <DockPanel>

        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open"/>
                <MenuItem Header="_Save">
                    <MenuItem Header="Save as _Doc/Docx" Command="{Binding SaveDocDocxFileCommand}"/>
                    <MenuItem Header="Save as _Pdf" Command="{Binding SavePdfFileCommand}"/>
                </MenuItem>
            </MenuItem>
        </Menu>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Recipes" FontSize="20" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="142,10,0,0"/>
            <TextBlock Text="Ingredients" FontSize="20" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,415,0"/>
            <TextBlock Text="Instructions" FontSize="20" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,124,0"/>



            <ListBox ItemsSource="{Binding Recipes}" SelectedItem="{Binding SelectedRecipe}" d:ItemsSource="{d:SampleData ItemCount=3}" HorizontalAlignment="Left" Width="250" FontSize="15" Margin="50,40,0,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock FontSize="18" Text="{Binding Name}"/>
                            <TextBlock Text="{Binding Type}"/>
                            <TextBlock Text="{Binding Cuisine}"/>
                            <Image Source="{Binding ImagePath}" HorizontalAlignment="Left" Height="50" Width="50" Stretch="Fill"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <ListBox ItemsSource="{Binding SelectedRecipe.Ingredients}" HorizontalAlignment="Right" FontSize="15" d:ItemsSource="{d:SampleData ItemCount=5}" BorderBrush="{x:Null}" Height="auto" Width="230" Background="#FFDAD4D3"  Margin="0,40,350,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Margin="0,0,20,0"/>
                            <TextBlock Text="{Binding Quantity}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <ListBox ItemsSource="{Binding SelectedRecipe.Instructions}" HorizontalAlignment="Right" FontSize="15" d:ItemsSource="{d:SampleData ItemCount=5}" BorderBrush="{x:Null}" Height="auto" Width="350" Background="#FFDAD4D3" Margin="0,40,0,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="Add Recipe" Command="{Binding AddRecipeCommand}" Margin="5"/>
                <Button Content="Remove Recipe" Command="{Binding RemoveRecipeCommand}" CommandParameter="{Binding SelectedRecipe}" Margin="5"/>
                <Button Content="Edit Recipe" Command="{Binding EditRecipeCommand}" CommandParameter="{Binding SelectedRecipe}" Margin="5"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5,20,0,0">
                <Button Content="Search" FontSize="10" Width="39" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding ToggleSearchBarCommand}"/>
                <TextBox Width="150" HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="{Binding SearchBarVisibility}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            
            <!--<StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5,60,0,0">
                
                <Button Content="Sort" FontSize="10" Width="39" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding ToggleSortBarCommand}"/>
                <StackPanel Orientation="Vertical" Background="#FFDDDDDD" Visibility="{Binding SortBarVisibility}">
                    <TextBlock Text="Type" FontSize="15" HorizontalAlignment="Center"/>
                    <ComboBox ItemsSource="{Binding RecipeTypeNames}" SelectedItem="{Binding SortType}" Width="130" Margin="5,0" FontSize="15" MaxDropDownHeight="100" IsTextSearchEnabled="True" />
                    <TextBlock Text="Cuisine" FontSize="15" HorizontalAlignment="Center" Margin="0,5"/>
                    <ComboBox ItemsSource="{Binding RecipeCuisineNames}" SelectedItem="{Binding SortCuisine}" Width="130" Margin="5,0" FontSize="15" MaxDropDownHeight="100" IsTextSearchEnabled="True" />
                    <Button Content="Reset" FontSize="15" HorizontalAlignment="Stretch" Margin="20,20,20,5" Command="{Binding ResetSortBarCommand}"/>

                </StackPanel>
                
            </StackPanel>-->

        </Grid>
        
    </DockPanel>
</Window>
