<Window x:Class="CookbookApplication.Views.RecipeEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookbookApplication.Views"
        mc:Ignorable="d"
        Title="RecipeEditView" Height="500" Width="900" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"></Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,5,0"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel Margin="5,5,600,5" MinHeight="400" MinWidth="300">
            <TextBlock Text="Name" FontWeight="DemiBold"/>
            <TextBox Text="{Binding Name}" FontSize="15"/>
            <TextBlock Text="Type" FontWeight="DemiBold"/>
            <TextBox Text="{Binding Type}" FontSize="15"/>
            <TextBlock Text="Cuisine" FontWeight="DemiBold"/>
            <TextBox Text="{Binding Cuisine}" FontSize="15"/>
            <Image Source="{Binding Image}" Height="150" Width="150" Stretch="Fill" Margin="0,10,0,0"/>
        </StackPanel>
        <ListBox ItemsSource="{Binding Ingredients}" Margin="305,0,0,0" FontSize="15">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding Name}" Margin="0,0,0,10"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
